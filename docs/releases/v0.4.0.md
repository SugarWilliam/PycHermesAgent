# PycHermesAgent v0.4.0 Release Notes

> Tag note for the `v0.4.0` release line. For current contracts and axes, prefer `docs/architecture/Compatibility_Matrix.md` and `docs/deployment/Production_Release_Gates.md`.

**Status:** Engineering preview (tranche-one work continues toward Phase 4 closure). Not production-ready.

## Build metadata

- **Tagged release:** `v0.4.0` on branch `main`
- **Package `__version__` / Desktop `package.json`:** `0.4.0`
- **Contract axes:** unchanged versus prior matrix row unless separately noted (`sidecar_api_version` **`0.10`**, `contract_version` **`0.5`**, etc.)

## Highlights

### Governance and roadmap clarity

- **Phase 5 evolution blueprint:** `docs/architecture/Phase5_Evolution_Blueprint_v0.5.0.md` — north star toward an all‑day digital teammate posture; **`Program tranche one` closes at Phase 4**, Phase 5 is a **fork evaluation** (`llm_gateway` vs upstream gateway), not tranche‑one backlog smuggling.
- **Aligned authority chain:** updates to `Phase3_Phase4_Productization_Roadmap` §11, `Phase3_Phase4_Exit_Checklist`, `Project_Development_and_Release_Governance` (Purpose + Phase 5 subsection + **Gate 9**), **`Compatibility_Matrix` §§3–6**, `Evolution_Backlog_Priorities` (scoped to tranche‑one), **`Production_Release_Gates` Gate 9** operational checklist.

### Retrieval and formal-analysis quality

- **IPC-oriented consistency heuristics** in `meta_harness/consistency.py` merged into **`build_evidence_validation_pack` → `logic_signals`** (resolution / latency‑ms / ONVIF‑vs‑proprietary wording windows).
- **IPC business MRAG benchmark** corpus extended (`benchmarks/mrag/ipc_semantic_benchmark.py`); deterministic hybrid≠lexical top‑1 checks preserved.

### Desktop / product UX

- **Context panel formal snapshot:** prominent degraded banner, harness risks, and **`analysis_card.evidence_chain.validation`** summary (conflicts, logic/temporal signals, policy hints, verification steps).

### Sidecar / contracts (incremental)

- Continued tightening of preference/materialize/skill‑patch‑draft/evolution‑ROI‑style surfaces reflected in **`sidecar_client`**, contracts/schemas, AgentLoop/session paths, MRAG/normalize/memory/chat services (`git log` diff for authoritative file list).

## Release automation

- **`.github/workflows/release.yml`** uses `body_path: docs/releases/${{ github.ref_name }}.md` so future tags pick the matching notes file (`vX.Y.Z` → `docs/releases/vX.Y.Z.md`).

## Verification

- `uv run pytest tests/contract -q` (**396 passed**) on HEAD before tagging.
- `uv run ruff check` on touched Python modules recommended.

## Known limitations

- Same honesty as **`Production_Release_Gates.md` §Honest scope** and **`Compatibility_Matrix` §5** — signing, neural defaults, Phase 5 program **not opened** unless explicitly chartered.
