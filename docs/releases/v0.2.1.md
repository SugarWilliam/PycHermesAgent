# PycHermesAgent v0.2.1

## Highlights

- **事实校对版评估** 纳入仓库：`docs/assessment/Reassessment_v4_FactChecked_and_Upgraded.md`
- **Phase 2 朝向 GA** 工作草案：`docs/architecture/Phase2_Toward_GA_v0.2.1.md`
- **Windows 侧车 onefile**：`pyc-hermes-sidecar.exe` 由 **`.github/workflows/release.yml`** 在推送本标签时构建并上传到 **GitHub Release**
- 文档：**`docs/deployment/Windows_Sidecar_Binary.md`**（运行时与限制说明）

## 获取二进制

在本 Release 的 **Assets** 中下载 **`pyc-hermes-sidecar.exe`** 及 **`.sha256`** 校验文件。

## 完整性校验（示例）

```powershell
Get-FileHash .\pyc-hermes-sidecar.exe -Algorithm SHA256
# 对照 *.sha256 中记录的值
```

## 范围

本版本 **不** 声明生产/商店级就绪；仍为 **engineering preview → GA 路径上的可发布构建**。参见 `AGENTS.md` 与 Phase 1/2 文档。
