# Project Cursor Skills

This directory imports project skills from `.opencode/skills` so Cursor can discover and use them as project-level skills.

Imported project skills exclude the vendored `software-project/superpowers` tree because Cursor already provides those superpowers skills through the installed Cursor plugin.

Source of truth for these imported skills is currently `.opencode/skills`; keep this directory in sync when OpenCode skills change.
